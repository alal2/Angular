'use strict';

describe('myApp.ngRepeat module', function() {

  beforeEach(module('myApp.ngRepeat'));

  describe('ngRepeat controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ngRepeatCtrl = $controller('repeatController');
      expect(ngRepeatCtrl).toBeDefined();
    }));

  });
});